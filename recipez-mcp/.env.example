# Recipez MCP Server Configuration

# ==================== REQUIRED ====================

# Recipez API base URL (HTTPS required)
RECIPEZ_BASE_URL=https://api.recipez.example.com

# Recipez JWT token for authentication
# Obtain this token by authenticating with the Recipez API
# Token format: Bearer <your-jwt-token>
RECIPEZ_JWT_TOKEN=your_jwt_token_here

# ==================== OPTIONAL ====================

# Server host (default: 0.0.0.0)
HOST=0.0.0.0

# Server port (default: 8000)
PORT=8000

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# CORS origins (comma-separated or "*" for all, default: "*")
# Production example: CORS_ORIGINS=https://claude.ai,https://app.recipez.example.com
CORS_ORIGINS=*
